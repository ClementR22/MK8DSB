import { StatName } from "@/types/statsTypes";

export const MAX_STAT_VALUE = 6;

export const CHOSEN_STATS_DEFAULT_SELECTED: StatName[] = ["speedGround", "acceleration", "miniTurbo"];

export const RESULTS_NUMBER_DEFAULT = 5;

export const SORT_NUMBER_SAVED_BUILDS_DEFAULT = 2;

export const IS_RESULT_STATS_SYNC = false;
